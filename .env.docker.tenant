# Docker / K8s 合并部署（子路径 /movie-editor，前后端同域）
VITE_APP_TYPE = tenant

VITE_USER_NODE_ENV = production

# 线上: https://api.highlands.ltd/movie-editor
VITE_PUBLIC_PATH = /movie-editor/

VITE_ROUTER_MODE = hash

VITE_BUILD_COMPRESS = none
VITE_BUILD_COMPRESS_DELETE_ORIGIN_FILE = false
VITE_DROP_CONSOLE = true

# 容器内不建议启用 PWA（避免 Service Worker 缓存干扰发布）
VITE_PWA = false

# SaaS 主站 API（构建时可由 --build-arg 覆盖）
VITE_API_URL = https://ext.highlands.ltd/light-sass-api/

# 与后端同域子路径：/movie-editor/api、/movie-editor/uploads
VITE_EDITOR_SERVER_URL = /movie-editor
VITE_EDITOR_FRONTEND_URL = /movie-editor
